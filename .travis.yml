sudo: required
language: generic

services:
  - docker

before_install:
  - docker build -t hugodiwang/docker-react -f Dockerfile .

script:
  - docker run -e CI=true hugodiwang/docker-react npm run test

deploy:
  provider: elasticbeanstalk
  region: 'us-east-2'
  app: 'docker-react'
  env: 'Dockerreact-env'
  bucket_name: 'elasticbeanstalk-us-east-2-421479510801'
  bucket_path: 'docker'
  on:
    branch: master
  access_key_id: $AWS_ACCESS_KEY
  secret_access_key: $AWS_SECRET_KEY
